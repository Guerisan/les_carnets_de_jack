{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Inscription{% endblock %}

{% block bodyClass %}user_form{% endblock %}
{% block body %}
    <h1>Créer un compte</h1>

    {{ form_start(registrationForm) }}
    {{ form_row(registrationForm.username) }}
    {{ form_row(registrationForm.email) }}
    {{ form_row(registrationForm.plainPassword) }}
    {{ form_row(registrationForm.profile_pic) }}

    {{ form_label(registrationForm.presentation, "Une courte présentation ? ") }}
    <p>Pense à mettre ton blog ou ton instagram (si tu en as un)! <br> Et si on s'est déjà rencontrés, n'oublie pas de le mentionner ;-)</p>
    {{ form_widget(registrationForm.presentation) }}

    <button class="btn">Envoyer</button>
    {{ form_end(registrationForm) }}
{% endblock %}
